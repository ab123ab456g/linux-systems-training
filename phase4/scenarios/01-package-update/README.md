# Scenario 01 — Package Update

目前 `report-helper` 為 1.0。Change Request 要求升級到 1.1，更新後 Report System 必須正常。

第二輪可嘗試 1.2-broken，觀察失敗並 rollback。

學生應自行執行：observe version → backup state → install target package → verify → rollback if necessary。
